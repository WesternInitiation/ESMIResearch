# ESMI Research (web)

Next.js compression lab for Vercel. See the [repository README](../README.md) for
full deploy and Supabase instructions.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Vercel **Root Directory** must be set to `web`.
